const fs = require('fs'); const lines = fs.readFileSync('C:/Users/uwemd/.gemini/antigravity-ide/brain/7a85ab67-4ea4-4d7a-a386-d5a4e609cc01/.system_generated/logs/transcript_full.jsonl', 'utf-8').split('\n'); for(const line of lines) { if(!line) continue; const obj = JSON.parse(line); if(obj.type === 'USER_INPUT' && obj.attachments && obj.attachments.length > 0) { console.log(obj.created_at, obj.attachments); } }
